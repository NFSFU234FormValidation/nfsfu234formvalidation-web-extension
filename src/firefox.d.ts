declare namespace browser {
    namespace runtime {
        function getURL(path: string): string;
        // Add other functions you need from the browser.runtime namespace
    }

    namespace storage {
        // Define the storage namespace with its properties and methods
        // Similar to the chrome.storage example
    }

    // Add other namespaces and functions as needed
}
